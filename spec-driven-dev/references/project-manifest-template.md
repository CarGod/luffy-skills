# {项目名称} — Project Manifest

> **🧭 本文件是 AI 理解此项目的入口。新对话窗口请先阅读此文件。**
>
> **版本**：v1.0
> **创建日期**：{YYYY-MM-DD}
> **最后更新**：{YYYY-MM-DD}

---

## 1. 项目简介

- **一句话定位**：{项目定位}
- **当前状态**：{规划中 / 开发中 / 已上线}
- **代码仓库**：{地址}

---

## 2. 技术栈速查

| 层级 | 技术 | 版本 |
|------|------|------|
| 前端 | {技术} | {版本} |
| 后端 | {技术} | {版本} |
| 数据库 | {技术} | {版本} |
| 缓存 | {技术} | {版本} |
| 部署 | {技术} | — |

---

## 3. 目录结构与文件职责

> ⚠️ **AI 开发新功能前，必须先查阅此目录索引，确认是否已有相关实现。**

```
{项目名}/
├── project-manifest.md            # 本文件 — 项目全局清单
├── specs/                         # 📋 规范文档
│   ├── spec.md                    # 主规范文档
│   ├── spec-database.md           # 数据库设计（如有）
│   ├── spec-api.md                # API 接口设计（如有）
│   ├── spec-ui.md                 # 前端页面设计（如有）
│   ├── spec-logic.md              # 核心业务逻辑（如有）
│   └── spec-integration.md        # 第三方集成（如有）
├── src/
│   ├── {模块A}/                   # {职责一句话}
│   │   ├── {子目录或文件}          # {职责说明}
│   │   └── ...
│   ├── {模块B}/                   # {职责一句话}
│   │   └── ...
│   ├── {模块C}/                   # {职责一句话}
│   │   └── ...
│   └── shared/                    # 🔧 公共工具 & 类型定义
│       ├── utils/                 # 通用工具函数
│       ├── types/                 # 共享类型定义
│       └── constants/             # 常量与配置
├── tests/                         # 🧪 测试文件
│   ├── unit/                      # 单元测试
│   └── integration/               # 集成测试
├── public/                        # 静态资源（如有）
├── .env.example                   # 环境变量模板
└── README.md                      # 项目说明
```

---

## 4. Spec 文档索引

| 文档 | 路径 | 内容摘要 | 状态 |
|------|------|---------|------|
| 主规范 | [specs/spec.md](./specs/spec.md) | {内容摘要} | {草稿/已确认/开发中} |
| 数据库 | [specs/spec-database.md](./specs/spec-database.md) | {内容摘要} | {状态} |
| API | [specs/spec-api.md](./specs/spec-api.md) | {内容摘要} | {状态} |
| UI | [specs/spec-ui.md](./specs/spec-ui.md) | {内容摘要} | {状态} |
| 逻辑 | [specs/spec-logic.md](./specs/spec-logic.md) | {内容摘要} | {状态} |
| 集成 | [specs/spec-integration.md](./specs/spec-integration.md) | {内容摘要} | {状态} |

> 只列出实际存在的文档，**删除未创建的行**。

---

## 5. 模块职责速查

> AI 在实现新功能前，先在此表中查找是否已有相关模块或实现。

| 模块名 | 目录 | 核心职责 | 关键文件 |
|--------|------|---------|---------|
| {模块A} | `src/{模块A}/` | {职责} | {关键文件列表} |
| {模块B} | `src/{模块B}/` | {职责} | {关键文件列表} |
| 公共工具 | `src/shared/` | 通用工具、类型、常量 | {关键文件列表} |

---

## 6. 开发进度追踪

### 当前里程碑：{里程碑名称}

| 任务 | 状态 | 负责模块 | 备注 |
|------|------|---------|------|
| {任务1} | ✅ 已完成 | {模块} | — |
| {任务2} | 🔨 进行中 | {模块} | {备注} |
| {任务3} | ⏳ 待开始 | {模块} | — |

---

## 7. 变更日志

> 每次 spec 文档修改或重大功能迭代，在此记录。

| 日期 | 变更内容 | 影响范围 |
|------|---------|---------|
| {YYYY-MM-DD} | 初始版本创建 | 全部 |
| {YYYY-MM-DD} | {变更描述} | {影响的模块/文档} |

---

## 8. 常见问题 & 约定

### 开发约定

- **添加新模块前**：先在本 manifest 中新增模块条目，再开始编码
- **修改数据模型后**：同步更新 `spec-database.md` 和本 manifest
- **新增第三方服务后**：更新 `spec-integration.md` 和 `.env.example`

### 环境配置

```bash
# 复制环境变量模板
cp .env.example .env

# 安装依赖
{包管理器} install

# 启动开发服务器
{启动命令}
```
