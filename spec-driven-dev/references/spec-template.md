# {项目名称} — 技术规范文档

> **版本**：v1.0
> **创建日期**：{YYYY-MM-DD}
> **最后更新**：{YYYY-MM-DD}
> **状态**：草稿 / 已确认 / 开发中 / 已完成

---

## 1. 项目概述

### 1.1 一句话定位

{用一句话说明这个系统是什么、为谁、解决什么问题}

### 1.2 核心问题

{没有这个系统，用户面临的核心痛点是什么？}

### 1.3 目标用户

| 用户角色 | 描述 | 核心需求 |
|---------|------|---------|
| {角色1} | {简短描述} | {核心诉求} |
| {角色2} | {简短描述} | {核心诉求} |

### 1.4 MVP 范围

**本版本做：**
- {功能1}
- {功能2}
- {功能3}

**本版本不做：**
- {功能A}（原因：{原因}）
- {功能B}（原因：{原因}）

---

## 2. 技术栈与约束

### 2.1 技术选型

| 层级 | 技术 | 版本 | 选型理由 |
|------|------|------|---------|
| 前端 | {如 React} | {如 18.x} | {理由} |
| 后端 | {如 NestJS} | {如 10.x} | {理由} |
| 数据库 | {如 PostgreSQL} | {如 16} | {理由} |
| 缓存 | {如 Redis} | {如 7.x} | {理由} |
| 部署 | {如 Docker + Vercel} | — | {理由} |

### 2.2 开发约束

- **代码风格**：{如 ESLint + Prettier, 统一使用 TypeScript strict mode}
- **命名规范**：{如文件名 kebab-case, 组件名 PascalCase}
- **提交规范**：{如 Conventional Commits}
- **分支策略**：{如 main + feature branches}

---

## 3. 模块架构

### 3.1 模块总览

```
系统名称
├── 模块A（{职责一句话}）
├── 模块B（{职责一句话}）
├── 模块C（{职责一句话}）
└── 模块D（{职责一句话}）
```

### 3.2 模块依赖关系

```
模块A ──→ 模块B ──→ 模块C
                └──→ 模块D
```

### 3.3 目录结构

```
{项目名}/
├── src/
│   ├── {模块A}/           # {职责说明}
│   │   ├── {子目录}/      # {职责说明}
│   │   └── ...
│   ├── {模块B}/           # {职责说明}
│   └── ...
├── specs/                  # 规范文档
├── tests/                  # 测试文件
├── project-manifest.md     # 项目清单
└── ...
```

---

## 4. 数据模型

{如果数据模型较复杂（≥3张表），此处只放概览表格，详细设计请拆分到子文档}

> 📖 **详细设计**：请查阅 [spec-database.md](./spec-database.md)

### 核心表概览

| 表名 | 职责 | 关键字段 |
|------|------|---------|
| {表1} | {一句话职责} | {关键字段列表} |
| {表2} | {一句话职责} | {关键字段列表} |
| {表3} | {一句话职责} | {关键字段列表} |

---

## 5. 核心逻辑

{如果业务逻辑复杂，此处只写核心流程概要，详细设计请拆分到子文档}

> 📖 **详细设计**：请查阅 [spec-logic.md](./spec-logic.md)

### 核心流程

```
{用简单的流程图或状态机描述核心业务流程}
例如：
用户注册 → 创建订单 → 支付 → 确认 → 使用 → 结算
                           ↓
                        支付失败 → 取消
```

### 关键业务规则

1. **{规则名称}**：{规则描述}
2. **{规则名称}**：{规则描述}

---

## 6. 接口设计

{如果接口较多（≥5个），此处只放概览，详细设计请拆分到子文档}

> 📖 **详细设计**：请查阅 [spec-api.md](./spec-api.md)

### 核心接口概览

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/{资源} | {说明} |
| GET | /api/{资源} | {说明} |
| PUT | /api/{资源}/:id | {说明} |
| DELETE | /api/{资源}/:id | {说明} |

---

## 7. 前端页面

{如果页面较多（≥3个），此处只放概览，详细设计请拆分到子文档}

> 📖 **详细设计**：请查阅 [spec-ui.md](./spec-ui.md)

### 页面清单

| 页面 | 路由 | 核心功能 |
|------|------|---------|
| {页面1} | /{路由} | {功能概述} |
| {页面2} | /{路由} | {功能概述} |

---

## 8. 子文档索引

| 文档 | 内容 | 路径 |
|------|------|------|
| UI 设计规范 | 色彩、字体、间距、动效、组件视觉规范 | [spec-design.md](./spec-design.md) |
| 数据库设计 | 表结构、索引、关系图 | [spec-database.md](./spec-database.md) |
| API 接口 | 接口详情、请求/响应体、错误码 | [spec-api.md](./spec-api.md) |
| 前端页面 | 页面布局、组件拆解、交互说明 | [spec-ui.md](./spec-ui.md) |
| 业务逻辑 | 状态机、流程图、业务规则详情 | [spec-logic.md](./spec-logic.md) |
| 第三方集成 | SDK、Webhook、鉴权方案 | [spec-integration.md](./spec-integration.md) |

> ⚠️ 只列出实际创建的子文档，**删除未使用的行**。

---

## 9. 阶段任务拆解

### Phase 1：基础设施（预计 {N} 天）

- [ ] 项目初始化、目录结构搭建
- [ ] 数据库建表和 migration
- [ ] 基础鉴权 / 用户体系

**产出物**：可运行的项目骨架 + 数据库 ready

### Phase 2：核心功能（预计 {N} 天）

- [ ] {核心功能1}
- [ ] {核心功能2}
- [ ] {核心功能3}

**产出物**：核心功能可用

### Phase 3：完善与对接（预计 {N} 天）

- [ ] 前端页面完善
- [ ] 接口联调
- [ ] 第三方服务对接

**产出物**：完整可使用的 MVP

---

## 10. 验收标准

### 功能验收

- [ ] {具体可测试的验收条件1}
- [ ] {具体可测试的验收条件2}
- [ ] {具体可测试的验收条件3}

### 技术验收

- [ ] 单元测试覆盖率 ≥ {N}%
- [ ] API 接口文档（Swagger / OpenAPI）完整
- [ ] 无 critical / high 级别安全漏洞
- [ ] 关键路径响应时间 < {N} ms
