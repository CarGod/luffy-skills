# {子文档标题} — {项目名称}

> 本文档是 [spec.md](spec.md) 的子文档，聚焦于 **{模块名称}** 的详细设计。

---

## 适用场景

> 根据子文档类型，选择以下对应的结构模板。一个子文档只使用一种类型。

---

## 模板 A：数据库设计 (spec-database.md)

### 表结构

#### {表名}

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| {字段名} | {VARCHAR(255)} | {NOT NULL} | {业务含义} |
| created_at | DATETIME | NOT NULL, DEFAULT NOW() | 创建时间 |
| updated_at | DATETIME | ON UPDATE NOW() | 更新时间 |

**索引**：
| 索引名 | 字段 | 类型 | 用途 |
|--------|------|------|------|
| {idx_user_dept} | {dept_id} | {NORMAL} | {按部门筛选} |

### 实体关系

```
{users} 1 ──→ N {orders}     (一个用户多个订单)
{orders} N ←── 1 {products}   (一个订单关联一个商品)
```

---

## 模板 B：API 接口设计 (spec-api.md)

### {接口组名称}

#### {接口名称}

| 属性 | 值 |
|------|-----|
| Method | `{POST}` |
| Path | `{/api/v1/xxx}` |
| 鉴权 | {AULP Token / 无} |
| 说明 | {一句话描述} |

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| {name} | {String} | {是} | {用户名} |

**响应**：
```json
{
  "code": 0,
  "data": { },
  "message": "success"
}
```

**错误码**：
| code | 含义 |
|------|------|
| {4001} | {参数校验失败} |
| {4003} | {权限不足} |

---

## 模板 C：UI 页面设计 (spec-ui.md)

### {页面名称}

| 属性 | 值 |
|------|-----|
| 路由 | `{/dashboard}` |
| 布局 | {左导航 + 右内容} |
| 核心功能 | {数据概览、统计图表} |

**页面结构**：
```
┌─────────────────────────────────────┐
│ Header（Logo + 用户头像）            │
├──────────┬──────────────────────────┤
│ SideNav  │ Main Content             │
│          │ ┌──────────────────────┐ │
│ - 首页   │ │ StatCard × 4         │ │
│ - 列表   │ ├──────────────────────┤ │
│ - 设置   │ │ Chart Area           │ │
│          │ ├──────────────────────┤ │
│          │ │ Recent Activity List │ │
│          │ └──────────────────────┘ │
└──────────┴──────────────────────────┘
```

**交互行为**：
| 触发 | 动作 | 反馈 |
|------|------|------|
| {点击统计卡片} | {跳转到对应列表页} | {路由切换} |
| {点击导出按钮} | {调用导出 API} | {Toast 提示 + 文件下载} |

---

## 模板 D：业务逻辑 (spec-logic.md)

### {流程名称}

**前置条件**：{用户已登录、有对应权限}

**正常流程**：
1. {用户在前端触发操作}
2. {前端调用 `POST /api/xxx`，传入 `{param1, param2}`}
3. {后端校验：检查 param1 是否合法}
4. {校验通过 → 写入数据库 `{table_name}`}
5. {返回 `code: 0` + 新记录 ID}

**异常分支**：
| 步骤 | 异常条件 | 处理方式 |
|------|---------|---------|
| 3 | {param1 格式非法} | {返回 `code: 4001`，提示具体错误} |
| 4 | {数据库唯一约束冲突} | {返回 `code: 4009`，提示重复} |

**状态机**（如有）：
```
[草稿] → 提交 → [待审核] → 通过 → [已发布]
                           → 驳回 → [已驳回] → 修改 → [待审核]
```
