# {主文档名称} — {子模块名称} 详细设计

> **所属主文档**：[spec.md](./spec.md)
> **版本**：v1.0
> **最后更新**：{YYYY-MM-DD}

---

## 1. 模块概述

**职责**：{一句话说明本子文档覆盖的范围}

**与其他模块的关系**：
- 被 {模块X} 调用：{说明}
- 依赖 {模块Y}：{说明}

---

<!-- 以下根据子文档类型，选择对应的章节模板 -->

<!-- ========== 数据库子文档 (spec-database.md) ========== -->

## 2. 数据表定义

### 2.1 {表名}

**职责**：{一句话}

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | {type} | PK, AUTO_INCREMENT | 主键 |
| {字段} | {type} | {约束} | {说明} |
| created_at | TIMESTAMP | NOT NULL, DEFAULT NOW() | 创建时间 |
| updated_at | TIMESTAMP | NOT NULL, DEFAULT NOW() | 更新时间 |

**索引**：
- `idx_{表名}_{字段}` — {用途}
- `uniq_{表名}_{字段}` — {用途}

**关系**：
- {表名}.{字段} → {另一表}.{字段}（{关系类型}）

### 2.2 {下一张表}

{同上格式}

---

<!-- ========== API 子文档 (spec-api.md) ========== -->

## 2. 接口详情

### 2.1 {接口名称}

- **方法**：{GET/POST/PUT/DELETE}
- **路径**：`/api/v1/{资源}`
- **描述**：{接口用途}
- **鉴权**：{是否需要认证、权限级别}

**请求参数：**

| 参数 | 位置 | 类型 | 必填 | 说明 |
|------|------|------|------|------|
| {param} | {body/query/path} | {type} | {是/否} | {说明} |

**请求示例：**

```json
{
  "field": "value"
}
```

**成功响应** (200)：

```json
{
  "code": 0,
  "data": { }
}
```

**错误码：**

| 错误码 | 说明 | 处理建议 |
|--------|------|---------|
| {code} | {说明} | {建议} |

### 2.2 {下一个接口}

{同上格式}

---

<!-- ========== 前端 UI 子文档 (spec-ui.md) ========== -->

## 2. 页面设计

### 2.1 {页面名称}

- **路由**：`/{route}`
- **布局**：{布局描述}
- **核心功能**：{功能列表}

**组件拆解：**

```
PageLayout
├── Header（导航栏）
├── {组件A}（{职责}）
│   ├── {子组件}
│   └── {子组件}
├── {组件B}（{职责}）
└── Footer
```

**关键交互：**

1. {用户操作} → {系统响应}
2. {用户操作} → {系统响应}

**状态管理：**

| 状态字段 | 类型 | 初始值 | 说明 |
|---------|------|--------|------|
| {field} | {type} | {default} | {说明} |

### 2.2 {下一个页面}

{同上格式}

---

<!-- ========== 业务逻辑子文档 (spec-logic.md) ========== -->

## 2. 状态机定义

### 2.1 {实体名称} 状态流转

**状态列表：**

| 状态 | 值 | 说明 |
|------|------|------|
| {STATUS_A} | {value} | {进入条件} |
| {STATUS_B} | {value} | {进入条件} |

**流转规则：**

```
{STATUS_A} ──(条件1)──→ {STATUS_B}
{STATUS_B} ──(条件2)──→ {STATUS_C}
{STATUS_B} ──(条件3)──→ {STATUS_D}  // 异常路径
```

**流转约束：**

1. {STATUS_A} → {STATUS_B}：需要 {前置条件}
2. {STATUS_B} → {STATUS_C}：触发 {副作用}（如发送通知、更新库存）

---

<!-- ========== 集成子文档 (spec-integration.md) ========== -->

## 2. 第三方服务集成

### 2.1 {服务名称}

- **用途**：{为什么需要集成该服务}
- **SDK/API**：{SDK 名称或 API 基础 URL}
- **鉴权方式**：{API Key / OAuth / 其他}

**核心调用：**

| 操作 | 方法 | 端点 | 说明 |
|------|------|------|------|
| {操作} | {method} | {endpoint} | {说明} |

**配置项：**

| 环境变量 | 说明 | 示例 |
|---------|------|------|
| {VAR_NAME} | {说明} | {示例值} |

**错误处理：**

| 场景 | 处理策略 |
|------|---------|
| 服务不可用 | {如重试 3 次后降级} |
| 鉴权失败 | {如报警 + 拒绝操作} |

---

## 3. 注意事项与约束

- {重要约束1}
- {重要约束2}
- {边界情况处理}
